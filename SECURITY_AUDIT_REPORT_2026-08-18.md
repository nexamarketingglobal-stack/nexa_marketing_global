# تقرير المراجعة الأمنية الشاملة

**المشروع:** Nexa Marketing Global  
**المستودع:** `nexamarketingglobal-stack/nexa_marketing_global`  
**نطاق المراجعة الأصلي:** commit `afa4f954` على الفرع `main`  
**تاريخ المراجعة:** 18 أغسطس 2026  
**المنهج:** مراجعة ساكنة للكود، فحص أسرار، تدقيق اعتمادات إنتاج، فحص إعدادات الخادم ورؤوس HTTP، والتحقق من البناء.

## النتيجة التنفيذية

بدأت المراجعة بإصدار يحوي ثغرات معروفة في شجرة اعتمادات الإنتاج، بما فيها **16 تحذيراً عالي الخطورة** و**47 تحذيراً متوسط الخطورة** و**8 تحذيرات منخفضة الخطورة** بحسب تدقيق الحزم. ارتبطت المخاطر الرئيسية بحزم غير مستخدمة مباشرةً مثل `axios` و`streamdown` و`nanoid`، وباعتمادات قديمة مرتبطة بـ `express` و`recharts`.

تمت إزالة الحزم والمكوّنات غير المستخدمة، وترقية `express` إلى `5.2.1`، وإضافة طبقة رؤوس حماية للمتصفح والخادم. أعاد تدقيق اعتمادات الإنتاج بعد المعالجة نتيجة **0 حرجة، 0 عالية، 0 متوسطة، و0 منخفضة**. كما نجح فحص TypeScript وبناء نسخة الإنتاج، وتم التحقق من رؤوس الحماية محلياً. سياسة أمن المحتوى (CSP) تحدد مصادر السكربتات والخطوط والصور والاتصالات المسموح بها، وهي آلية مصممة لتقييد تحميل الموارد وتقليل أثر هجمات XSS.[1]

| المجال | الحالة النهائية | التقييم |
| --- | --- | --- |
| أسرار أو مفاتيح ضمن المستودع | لم تظهر أنماط لمفاتيح أو كلمات مرور أو ملفات اعتماد متعقبة | سليم ضمن نطاق الفحص |
| ثغرات اعتمادات الإنتاج المعروفة | 0 حسب `pnpm audit --prod` بعد المعالجة | سليم حالياً |
| رؤوس حماية المتصفح | CSP وPermissions Policy وReferrer Policy وCOOP ورفض التضمين مفعّلة | محسّن |
| إدخال البريد الإلكتروني | تحقق محلي، حد أقصى 254 حرفاً، وإيقاف التقاطه في سجلات التطوير | محسّن |
| تخزين وإرسال البريد | لا توجد حالياً خدمة خلفية أو عملية إرسال فعلية | قرار مطلوب قبل جمع بيانات حقيقية |

## ما تم فحصه

شملت المراجعة ملفات الواجهة في `client/`، إعدادات Vite، خادم Express في `server/index.ts`، وملفات الاعتمادات `package.json` و`pnpm-lock.yaml`. كما فُحصت استجابة النطاق المنشور عبر HTTPS، وسجل التحقق رؤوس `Strict-Transport-Security` و`X-Content-Type-Options` الصادرة من الاستضافة قبل التعديلات. لا تظهر الواجهة الحالية تدفقات تسجيل دخول، أو دفع، أو API عامة، أو قاعدة بيانات، ولذلك يقتصر سطح الهجوم الحالي على الواجهة الثابتة ونموذج البريد والأصول الخارجية.

## النتائج والإصلاحات

| الأولوية | النتيجة قبل الإصلاح | الأثر المحتمل | الإجراء المنفذ | الحالة |
| --- | --- | --- | --- | --- |
| مرتفعة | وجود 16 تحذيراً عالي الخطورة في الاعتمادات، من بينها مسارات متأثرة عبر `axios` و`nanoid` و`express` و`recharts` و`streamdown` | توسعة سطح هجوم سلسلة التوريد، بما فيها تحذيرات تخص تسريب بيانات اعتماد أو ReDoS أو حقن كود ضمن النسخ المتأثرة | أزيلت `axios` و`streamdown` و`nanoid` و`recharts` غير المستخدمة، أزيل مكوّن chart غير المستخدم، ورُقي `express` إلى `5.2.1` | مغلقة؛ تدقيق الإنتاج النهائي بلا ثغرات معروفة |
| متوسطة | لم يكن الخادم يحدد CSP أو سياسة صلاحيات أو سياسة إحالة أو حماية صريحة من التضمين | حماية أضعف ضد XSS وclickjacking والتحميل غير المقصود للموارد | أضيفت رؤوس CSP و`Permissions-Policy` و`Referrer-Policy` و`X-Frame-Options` و`Cross-Origin-Opener-Policy`، وعُطّل `X-Powered-By` | مغلقة |
| منخفضة | حقل البريد لا يملك اسماً صريحاً أو حد طول أو توجيهات autocomplete، وكان جامع سجلات التطوير قد يلتقط مدخلات غير حساسة | احتمال التقاط عنوان بريد في سجلات التطوير أو قبول إدخال أطول من المطلوب | أضيفت `name="email"` و`inputMode="email"` و`autoComplete="email"` و`maxLength={254}` و`manus-no-record` | مغلقة |
| معلوماتية | روابط الفوتر القانونية (`Privacy` و`Terms` و`Cookies`) ما زالت مؤشرات `#` | لا توجد وثائق قانونية مرئية إذا تقرر جمع بيانات حقيقية | لم تُنشأ وثائق افتراضية لأن مضمونها يعتمد على طريقة التخزين والبريد ومزود الخدمة الذي سيختاره المالك | مفتوحة قبل تشغيل جمع البيانات |
| معلوماتية | نموذج الاشتراك يعرض نجاحاً بعد مؤقت محلي، من دون طلب شبكة أو خدمة تخزين أو إرسال فعلي | لا يوجد تسرب بيانات حالياً، لكنه لا يجمع العملاء المحتملين فعلياً وقد يكون الوعد برسالة بريد غير دقيق | لم يُربط النموذج بخدمة خارجية أو قاعدة بيانات لتجنب إدخال معالجة بيانات شخصية من دون قرار صريح | قرار مطلوب |

> **تنبيه تشغيلي:** النموذج الحالي لا يرسل البريد إلى خادم ولا يحتفظ به. لا تشغّل حملة تجمع عناوين حقيقية أو تعد المستخدم برسالة بريد حتى تضاف خدمة خلفية آمنة، وموافقة صريحة، وسياسة خصوصية فعلية، وإجراء إلغاء اشتراك.

## الضوابط المطبقة

تحدد سياسة أمن المحتوى `default-src 'self'` مع قوائم سماح ضيقة للموارد اللازمة فقط، مثل خطوط Google وAnalytics الخاص بالاستضافة وأصول التخزين الموقعة. كما تعطل السياسة `object-src`، وتقصر `form-action` على المصدر نفسه، وتمنع تضمين الموقع في إطار عبر `frame-ancestors 'none'`. تشرح MDN أن CSP تتيح لصاحب الموقع التحكم في مصادر الموارد المسموح للمتصفح بتحميلها، بما يحد من آثار حقن السكربتات عبر المواقع.[1]

كما عطّلت `Permissions-Policy` وصول الصفحة إلى الكاميرا والميكروفون والموقع والدفع وUSB، وهي سياسة تمكّن الخادم من السماح أو المنع لخصائص متصفح محددة.[2] وتتماشى إزالة ترويسة `X-Powered-By` وتحديث Express مع توصيات Express لتقليل البصمة التقنية والحفاظ على الاعتمادات خالية من الثغرات المعروفة.[3]

## التحقق المنفذ

| التحقق | النتيجة |
| --- | --- |
| فحص أنماط الأسرار ضمن الملفات المتعقبة | لم تظهر مفاتيح API أو رموز وصول أو مفاتيح خاصة أو ملفات اعتماد متعقبة |
| `pnpm audit --prod` قبل المعالجة | 16 عالية، 47 متوسطة، 8 منخفضة، 0 حرجة |
| `pnpm audit --prod` بعد المعالجة | 0 حرجة، 0 عالية، 0 متوسطة، 0 منخفضة |
| `pnpm check` | ناجح؛ لا أخطاء TypeScript |
| `pnpm build` | ناجح؛ تم بناء الواجهة والخادم بنجاح |
| اختبار رؤوس خادم الإنتاج المبني محلياً | CSP وPermissions Policy وReferrer Policy و`X-Content-Type-Options` و`X-Frame-Options` وCOOP حاضرة |
| تحقق بصري من الصفحة | الأصول وروابط التخزين الموقعة تعمل بعد إضافة نطاق CloudFront المخصص للصور إلى `img-src` |

## مخاطر متبقية وخطة الأولوية

تظل المخاطر الحالية منخفضة في النسخة المنشورة لأنها لا تتضمن حسابات مستخدمين أو مدفوعات أو تخزين بيانات. مع ذلك، يتحول الوضع فور ربط نموذج الاشتراك بخدمة تجمع البريد؛ عندها يجب ترقية المشروع إلى بنية خلفية، وتطبيق تحقق خادمي مستقل عن المتصفح، وتحديد حد للطلبات لكل IP، وتسجيل موافقة الاشتراك، وتفعيل double opt-in، وإضافة صفحة خصوصية وسياسة ملفات تعريف ارتباط حقيقية قبل إعلان النموذج كقناة لجمع العملاء المحتملين.

ينبغي أيضاً مراقبة تحديثات الحزم دورياً في GitHub، وتشغيل `pnpm audit --prod` في CI قبل الدمج. يوصي دليل Express بتجنّب الإصدارات الضعيفة والاحتفاظ بالاعتمادات تحت المراقبة المستمرة.[3]

## المراجع

[1] [MDN — Content-Security-Policy](https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Headers/Content-Security-Policy)  
[2] [MDN — Permissions-Policy](https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Headers/Permissions-Policy)  
[3] [Express — Production Best Practices: Security](https://expressjs.com/en/advanced/best-practice-security.html)
